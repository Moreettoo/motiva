# Fase 3 · App de campo — plano de implementação

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Dar ao líder de equipe uma tela em `/campo` que abre sem sinal, lista os chamados da equipe, registra início (2 fotos), fim (2 fotos + altura) e pedido de adiamento numa fila local que sincroniza sozinha quando a rede volta, e empacotá-la como APK instalável.

**Architecture:** Grupo de rotas `(campo)` com layout próprio e página cliente pura, estática, precacheada por um service worker (`@serwist/turbopack`). Dados em IndexedDB (`idb`): snapshot do estado, fila de eventos com `evento_id` gerado no aparelho, fotos como blob comprimido. Rotas de API no próprio painel (`/api/campo/*`), autenticadas pelo cookie de sessão, escrevem com a chave secreta chamando `ia.registrar_evento_chamado` com origem `campo`. O servidor tem a palavra final; evento para chamado terminal volta como fora de ordem e não trava a fila. APK por Trusted Web Activity gerado no PWABuilder.

**Tech Stack:** os da Fase 1 e 2, mais `idb`, `serwist`, `@serwist/turbopack` (dev), `esbuild` (dev), `sharp` (dev, ícones), `qrcode` (QR do APK). Android 10+ com Chrome.

**Spec:** `docs/superpowers/specs/2026-09-09-acesso-e-chamados-design.md`, "§3 · App de campo". Pré-requisitos: Fases 1 e 2 concluídas (sessão, `perfis`, `equipes.lider_id`, tabelas e funções de chamados, `/api/fotos/[id]`).

## Global Constraints

- Tudo das fases anteriores continua valendo.
- **`/campo` não lê cookie, header nem banco na renderização.** A página é estática. Quem lê é a API, quem guarda é o IndexedDB.
- **Todo `evento_id` e `foto_id` nascem no aparelho** (`crypto.randomUUID()`) e nunca mudam. Reenviar é sempre seguro.
- **Fotos sobem antes do evento** a que pertencem. O servidor recusa `iniciado`/`finalizado` sem as duas fotos nomeadas.
- Nada sai da fila sem `200` do servidor, exceto `fora_de_ordem`, que sai e vira aviso.
- Botões de ação com pelo menos 56 px de altura; texto base 17 px; contraste do tema escuro; nenhum spinner sem palavra.
- Sem `localStorage` para dado de trabalho: só IndexedDB. `sair` apaga IndexedDB e caches.
- Testar offline sempre com `npm run build && npm run start`: em `next dev` o service worker não representa produção.

---

## Estrutura de arquivos

| Arquivo | Ação | Responsabilidade |
|---|---|---|
| `web/package.json` | alterar | Dependências e script `icones` |
| `web/next.config.ts` | alterar | `withSerwist(...)` |
| `web/src/app/manifest.ts` | criar | Manifesto da PWA |
| `web/public/icones/campo.svg`, `campo-192.png`, `campo-512.png`, `campo-512-maskable.png`, `qr-apk.svg` | criar | Ícones e QR (PNG e QR gerados por script) |
| `web/scripts/gerar-icones.mjs` | criar | SVG → PNG com `sharp`; QR do APK com `qrcode` |
| `web/src/app/sw.ts` | criar | Service worker: precache, fallback de `/campo`, `sync` |
| `web/src/app/serwist/[path]/route.ts` | criar | Serve o SW compilado com o manifesto de precache |
| `web/src/components/campo/serwist-provider.tsx` | criar | Reexporta o provider cliente |
| `web/src/app/(campo)/layout.tsx` | criar | Tela cheia, tema escuro, provider do SW |
| `web/src/app/(campo)/campo/page.tsx` | criar | Casca estática |
| `web/src/lib/campo/contratos.ts` | criar | Tipos compartilhados entre API e cliente |
| `web/src/lib/campo/banco-local.ts` | criar | IndexedDB via `idb` |
| `web/src/lib/campo/fila.ts` + `.test.ts` | criar | Regras puras da fila e do estado local |
| `web/src/lib/campo/imagem.ts` + `.test.ts` | criar | Redimensionar e comprimir; GPS |
| `web/src/lib/campo/sincronizar.ts` | criar | Motor de envio (página e worker) |
| `web/src/lib/campo/servidor.ts` | criar | Helpers das rotas: equipe do usuário, autorização do chamado |
| `web/src/app/api/campo/estado/route.ts`, `fotos/route.ts`, `eventos/route.ts` | criar | API do campo |
| `web/src/app/(campo)/campo/_componentes/app-campo.tsx`, `usar-sincronizacao.ts`, `cabecalho-campo.tsx`, `indicador-sincronizacao.tsx`, `lista-chamados.tsx`, `cartao-chamado.tsx`, `detalhe-chamado.tsx`, `captura-foto.tsx`, `fluxo-iniciar.tsx`, `fluxo-finalizar.tsx`, `fluxo-adiar.tsx`, `tela-revisao.tsx` | criar | A tela |
| `web/public/.well-known/assetlinks.json` | criar | Verificação de propriedade do APK (conteúdo vem do PWABuilder) |
| `web/src/app/(painel)/usuarios/_componentes/baixar-app.tsx` | criar | Cartão com QR e instruções |
| `docs/operacao/apk.md` | criar | Como gerar e atualizar o APK; onde fica a chave |

---

### Task 1: Dependências, manifesto e ícones

**Files:**
- Modify: `web/package.json`, `web/next.config.ts`
- Create: `web/src/app/manifest.ts`, `web/public/icones/campo.svg`, `web/scripts/gerar-icones.mjs`

- [ ] **Step 1: Instalar**

```bash
cd /e/motiva/web && npm install idb serwist qrcode && npm install -D @serwist/turbopack esbuild sharp @types/qrcode && cat node_modules/@serwist/turbopack/README.md | head -120
```
Leia o README instalado: a API abaixo (`withSerwist`, `createSerwistRoute`, `SerwistProvider`, `@serwist/turbopack/worker`) é a da versão 9; se o README divergir, o README manda.

- [ ] **Step 2: Manifesto**

Crie `web/src/app/manifest.ts`:
```ts
import type { MetadataRoute } from "next";

/* Segunda excecao a regra do hex (a primeira e o theme-color em layout.tsx, pelo
   mesmo motivo): manifesto nao le variavel CSS. #0a0d0c espelha --bg do tema escuro. */
export default function manifest(): MetadataRoute.Manifest {
  return {
    id: "/campo",
    name: "HighwAI Campo",
    short_name: "Campo",
    description: "Chamados de roçada da sua equipe, com ou sem sinal.",
    start_url: "/campo",
    scope: "/",
    display: "standalone",
    orientation: "portrait",
    lang: "pt-BR",
    background_color: "#0a0d0c",
    theme_color: "#0a0d0c",
    icons: [
      { src: "/icones/campo-192.png", sizes: "192x192", type: "image/png" },
      { src: "/icones/campo-512.png", sizes: "512x512", type: "image/png" },
      { src: "/icones/campo-512-maskable.png", sizes: "512x512", type: "image/png", purpose: "maskable" },
    ],
  };
}
```
O Next serve isto em `/manifest.webmanifest` e injeta o `<link rel="manifest">` sozinho.

- [ ] **Step 3: Ícone e script**

`web/public/icones/campo.svg`: a `Marca` (`components/shell/marca.tsx`) em 512×512, fundo `#0a0d0c`, traços em `#f7f7f4`, filete em `#a3e635`, com 12% de margem na versão maskable. Crie `web/scripts/gerar-icones.mjs`:
```js
import { writeFileSync, readFileSync } from "node:fs";
import sharp from "sharp";
import QRCode from "qrcode";

const svg = readFileSync(new URL("../public/icones/campo.svg", import.meta.url));
for (const [nome, tamanho, margem] of [["campo-192.png", 192, 0], ["campo-512.png", 512, 0], ["campo-512-maskable.png", 512, 0.12]]) {
  const util = Math.round(tamanho * (1 - 2 * margem));
  const icone = await sharp(svg).resize(util, util).png().toBuffer();
  await sharp({ create: { width: tamanho, height: tamanho, channels: 4, background: "#0a0d0c" } })
    .composite([{ input: icone, gravity: "center" }])
    .png()
    .toFile(new URL(`../public/icones/${nome}`, import.meta.url).pathname.replace(/^\/([A-Za-z]:)/, "$1"));
  console.log("ok", nome);
}

// QR do APK: o link e fixo por nome de arquivo, entao o QR pode ser estatico.
const link = "https://github.com/Moreettoo/motiva/releases/latest/download/campo.apk";
writeFileSync(new URL("../public/icones/qr-apk.svg", import.meta.url), await QRCode.toString(link, { type: "svg", margin: 1, color: { dark: "#0c100e", light: "#ffffff" } }));
console.log("ok qr-apk.svg");
```
`package.json`: `"icones": "node scripts/gerar-icones.mjs"`. Rode e confira os quatro arquivos.

- [ ] **Step 4: `withSerwist`**

Em `web/next.config.ts`, importe `import { withSerwist } from "@serwist/turbopack";` e troque `export default nextConfig;` por `export default withSerwist(nextConfig);`.

- [ ] **Step 5: Commit**

```bash
npm run tipos && git add -A && git commit -m "feat(campo): manifesto, icones e serwist" && git push origin main
```

---

### Task 2: Service worker, casca estática de `/campo` e teste offline a frio

**Files:**
- Create: `web/src/app/sw.ts`, `web/src/app/serwist/[path]/route.ts`, `web/src/components/campo/serwist-provider.tsx`
- Create: `web/src/app/(campo)/layout.tsx`, `web/src/app/(campo)/campo/page.tsx`, `web/src/app/(campo)/campo/_componentes/app-campo.tsx` (versão mínima)

- [ ] **Step 1: Service worker**

Crie `web/src/app/sw.ts`:
```ts
import { defaultCache } from "@serwist/turbopack/worker";
import type { PrecacheEntry, SerwistGlobalConfig } from "serwist";
import { Serwist } from "serwist";

import { sincronizarFila } from "@/lib/campo/sincronizar";

declare global {
  interface WorkerGlobalScope extends SerwistGlobalConfig {
    __SW_MANIFEST: (PrecacheEntry | string)[] | undefined;
  }
}
declare const self: ServiceWorkerGlobalScope;

type EventoSync = ExtendableEvent & { tag: string };

const serwist = new Serwist({
  precacheEntries: self.__SW_MANIFEST,
  skipWaiting: true,
  clientsClaim: true,
  navigationPreload: true,
  runtimeCaching: defaultCache,
  // Abrir a frio sem rede: qualquer navegacao sob /campo recebe a casca precacheada.
  fallbacks: {
    entries: [
      {
        url: "/campo",
        matcher({ request }) {
          return request.mode === "navigate" && new URL(request.url).pathname.startsWith("/campo");
        },
      },
    ],
  },
});

serwist.addEventListeners();

/* Background Sync: o Chrome acorda o worker quando a rede volta, mesmo com o app
   fechado. Se ha uma aba aberta, ela sincroniza (tem o estado na tela); se nao,
   o proprio worker envia a fila. */
self.addEventListener("sync", (evento) => {
  const e = evento as EventoSync;
  if (e.tag !== "campo-sincronizar") return;
  e.waitUntil(
    self.clients.matchAll({ type: "window", includeUncontrolled: true }).then((abas) => {
      if (abas.length > 0) {
        abas.forEach((aba) => aba.postMessage({ tipo: "sincronizar" }));
        return;
      }
      return sincronizarFila({ origem: "worker" }).then(() => undefined);
    }),
  );
});
```
Crie `web/src/app/serwist/[path]/route.ts`:
```ts
import { createSerwistRoute } from "@serwist/turbopack";

/* A revisao muda a cada deploy para o precache de /campo nao servir casca velha
   apontando para chunks que nao existem mais. Na Vercel o id do deploy basta;
   fora dela, o instante do build. */
const revision = process.env.VERCEL_DEPLOYMENT_ID ?? String(Date.now());

export const { dynamic, dynamicParams, revalidate, generateStaticParams, GET } = createSerwistRoute({
  swSrc: "src/app/sw.ts",
  useNativeEsbuild: true,
  additionalPrecacheEntries: [{ url: "/campo", revision }],
});
```
Crie `web/src/components/campo/serwist-provider.tsx`:
```tsx
"use client";

export { SerwistProvider } from "@serwist/turbopack/react";
```
O `proxy.ts` da Fase 1 já exclui `serwist` do matcher.

- [ ] **Step 2: Layout e casca**

Crie `web/src/app/(campo)/layout.tsx`:
```tsx
import type { Viewport } from "next";

import { SerwistProvider } from "@/components/campo/serwist-provider";

/* Sem Shell, sem barra lateral, tema escuro fixo: sol forte pede fundo escuro e
   texto claro, e a paleta escura ja foi medida. `maximumScale: 1` evita o zoom
   acidental ao tocar num campo, comum com luva. */
export const viewport: Viewport = {
  themeColor: "#0a0d0c",
  colorScheme: "dark",
  width: "device-width",
  initialScale: 1,
  maximumScale: 1,
  viewportFit: "cover",
};

export default function LayoutCampo({ children }: { children: React.ReactNode }) {
  return (
    <SerwistProvider swUrl="/serwist/sw.js">
      <div data-theme="dark" className="min-h-dvh bg-bg text-ink text-[17px] leading-6 antialiased">
        {children}
      </div>
    </SerwistProvider>
  );
}
```
Crie `web/src/app/(campo)/campo/page.tsx`:
```tsx
import type { Metadata } from "next";

import { AppCampo } from "./_componentes/app-campo";

export const metadata: Metadata = { title: "Campo · HighwAI" };

/* Estatica de proposito: nada de cookie, header ou banco aqui. E isso que permite
   o service worker guardar esta pagina e o app abrir a frio sem sinal. */
export default function PaginaCampo() {
  return <AppCampo />;
}
```
Versão mínima de `app-campo.tsx` (`"use client"`): um `<main className="mx-auto max-w-lg px-4 py-6">` com o título "Campo" e a frase "Carregando os chamados da sua equipe…". A Task 6 substitui.

- [ ] **Step 3: Verificar o offline a frio**

```bash
cd /e/motiva/web && npm run build && npm run start
```
No Chrome: entrar como um líder do seed, abrir `http://localhost:3000/campo`, DevTools → Application → Service Workers (ativo) e Cache Storage (contém `/campo`). Marcar "Offline" em Network, fechar a aba, abrir nova aba em `/campo`: a casca aparece. Se vier a página de erro do Chrome, o precache não pegou `/campo`: confira `revision` e se `page.tsx` ficou estático no resumo do `next build` (símbolo `○`).

- [ ] **Step 4: Commit**

```bash
git add -A && git commit -m "feat(campo): service worker com precache da casca e rota estatica /campo" && git push origin main
```

---

### Task 3: Contratos, banco local e regras puras

**Files:**
- Create: `web/src/lib/campo/contratos.ts`, `banco-local.ts`, `fila.ts`, `fila.test.ts`, `imagem.ts`, `imagem.test.ts`

**Interfaces:**
- Produces:
  ```ts
  // contratos.ts
  export type TrechoCampo = { id: number; rodovia: string; km_inicio: number; km_fim: number; uf: string; sentido: string | null; especie: string; altura_limite_cm: number; latitude: number; longitude: number; observacoes: string | null };
  export type ChamadoCampo = { id: number; numero: string; status: StatusChamado; trecho: TrechoCampo; data_sugerida: string; prioridade: Prioridade; justificativa: string;
    altura_inicial_cm: number | null; altura_inicial_origem: "prevista" | "informada"; altura_final_cm: number | null; iniciado_em: string | null; finalizado_em: string | null;
    comentario_gestor: string | null; adiamento_pendente: { motivo: MotivoAdiamento; data_sugerida: string | null } | null; atualizado_em: string };
  export type EstadoCampo = { equipe: { id: number; nome: string }; lider: { nome: string }; chamados: ChamadoCampo[]; notificacoesNaoLidas: number; servidorEm: string; sincronizadoEm: string };
  export type TipoEventoCampo = "iniciado" | "finalizado" | "adiamento_solicitado";
  export type EventoCampo = { evento_id: string; chamado_id: number; tipo: TipoEventoCampo; payload: Record<string, unknown>; ocorrido_em: string };
  export type ResultadoEvento = { evento_id: string; situacao: "aplicado" | "repetido" | "fora_de_ordem" | "recusado"; erro?: string };
  export type Papel = "medida" | "extensao" | "resultado" | "extra";
  export type FotoLocal = { foto_id: string; evento_id: string; chamado_id: number; etapa: "inicio" | "fim"; papel: Papel; blob: Blob; largura_px: number; altura_px: number; bytes: number;
    latitude: number | null; longitude: number | null; precisao_m: number | null; capturada_em: string; enviada: boolean };
  export type ItemFila = EventoCampo & { fotos: string[]; tentativas: number; ultimo_erro: string | null; criado_em: string };
  export const LIMITES = { ladoMaximoPx: 1600, qualidadeJpeg: 0.82, fotosExtrasMax: 4, alturaMaxCm: 300, gpsTimeoutMs: 8000 } as const;
  // banco-local.ts
  export function banco(): Promise<IDBPDatabase<EsquemaCampo>>;
  export function lerEstado(): Promise<EstadoCampo | null>;  gravarEstado(e): Promise<void>;
  export function listarFila(): Promise<ItemFila[]>;  enfileirar(i: ItemFila): Promise<void>;  atualizarItem(i): Promise<void>;  removerDaFila(evento_id): Promise<void>;
  export function guardarFoto(f: FotoLocal): Promise<void>;  fotosDoEvento(evento_id): Promise<FotoLocal[]>;  marcarFotoEnviada(foto_id): Promise<void>;
  export function limparTudo(): Promise<void>;
  // fila.ts
  export function ordenarFila(itens: ItemFila[]): ItemFila[];
  export function aplicarPendencias(chamados: ChamadoCampo[], fila: ItemFila[]): ChamadoCampo[];   // estado otimista
  export function resumoPendencias(fila: ItemFila[]): { total: number; comErro: number };
  export function esperaAntesDaTentativa(tentativas: number): number;   // ms: 0, 2s, 8s, 30s, 120s (teto)
  export function agruparChamados(chamados: ChamadoCampo[], hoje: string): { hoje: ChamadoCampo[]; atrasados: ChamadoCampo[]; proximos: ChamadoCampo[]; aguardando: ChamadoCampo[]; recentes: ChamadoCampo[] };
  // imagem.ts
  export function dimensoesReduzidas(largura: number, altura: number, ladoMaximo?: number): { largura: number; altura: number };
  export async function comprimirFoto(arquivo: File): Promise<{ blob: Blob; largura: number; altura: number }>;
  export async function capturarPosicao(): Promise<{ latitude: number; longitude: number; precisao_m: number } | null>;
  ```

- [ ] **Step 1: Testes puros primeiro**

`fila.test.ts`: `ordenarFila` ordena por `criado_em`; `aplicarPendencias` leva `aberto` + item `iniciado` para `em_andamento`, `em_andamento` + `finalizado` para `aguardando_aprovacao`, `aberto` + `adiamento_solicitado` para `adiamento_solicitado`, ignora item de chamado ausente e mantém o resto; `resumoPendencias` conta itens e itens com `ultimo_erro`; `esperaAntesDaTentativa(0..5)` devolve `[0, 2000, 8000, 30000, 120000, 120000]`; `agruparChamados` separa por `data_sugerida` versus `hoje` e por status (`aguardando_aprovacao`/`devolvido` em "aguardando"; `concluido`/`cancelado` em "recentes"; `aberto`/`em_andamento`/`adiamento_solicitado` em hoje/atrasados/próximos).

`imagem.test.ts`: `dimensoesReduzidas(4000, 3000)` → `{1600, 1200}`; `(3000, 4000)` → `{1200, 1600}`; `(800, 600)` → inalterado; `(1600, 1600)` → inalterado.

Run: `npx vitest run src/lib/campo` → FAIL (módulos inexistentes).

- [ ] **Step 2: Implementar**

`fila.ts` usa `proximoStatus` de `@/lib/chamados/maquina` para o estado otimista:
```ts
export function aplicarPendencias(chamados: ChamadoCampo[], fila: ItemFila[]): ChamadoCampo[] {
  const porChamado = new Map(chamados.map((c) => [c.id, { ...c }]));
  for (const item of ordenarFila(fila)) {
    const c = porChamado.get(item.chamado_id);
    if (!c) continue;
    const novo = proximoStatus(c.status, item.tipo, null);
    if (novo) c.status = novo;
    if (item.tipo === "finalizado" && typeof item.payload.altura_final_cm === "number") c.altura_final_cm = item.payload.altura_final_cm;
  }
  return [...porChamado.values()];
}
export function esperaAntesDaTentativa(tentativas: number): number {
  return [0, 2_000, 8_000, 30_000][tentativas] ?? 120_000;
}
```
`banco-local.ts` com `idb`:
```ts
import { openDB, type DBSchema, type IDBPDatabase } from "idb";
import type { EstadoCampo, FotoLocal, ItemFila } from "./contratos";

interface EsquemaCampo extends DBSchema {
  estado: { key: string; value: EstadoCampo };
  fila: { key: string; value: ItemFila; indexes: { por_criacao: string } };
  fotos: { key: string; value: FotoLocal; indexes: { por_evento: string } };
}

let promessa: Promise<IDBPDatabase<EsquemaCampo>> | null = null;

export function banco() {
  promessa ??= openDB<EsquemaCampo>("highwai-campo", 1, {
    upgrade(db) {
      db.createObjectStore("estado");
      const fila = db.createObjectStore("fila", { keyPath: "evento_id" });
      fila.createIndex("por_criacao", "criado_em");
      const fotos = db.createObjectStore("fotos", { keyPath: "foto_id" });
      fotos.createIndex("por_evento", "evento_id");
    },
  });
  return promessa;
}
export async function lerEstado() { return (await (await banco()).get("estado", "atual")) ?? null; }
export async function gravarEstado(e: EstadoCampo) { await (await banco()).put("estado", e, "atual"); }
export async function listarFila() { return (await banco()).getAllFromIndex("fila", "por_criacao"); }
export async function enfileirar(i: ItemFila) { await (await banco()).put("fila", i); }
export async function atualizarItem(i: ItemFila) { await (await banco()).put("fila", i); }
export async function removerDaFila(eventoId: string) { await (await banco()).delete("fila", eventoId); }
export async function guardarFoto(f: FotoLocal) { await (await banco()).put("fotos", f); }
export async function fotosDoEvento(eventoId: string) { return (await banco()).getAllFromIndex("fotos", "por_evento", eventoId); }
export async function marcarFotoEnviada(fotoId: string) {
  const db = await banco(); const f = await db.get("fotos", fotoId);
  if (f) await db.put("fotos", { ...f, enviada: true });
}
export async function limparTudo() {
  const db = await banco();
  await Promise.all([db.clear("estado"), db.clear("fila"), db.clear("fotos")]);
}
```
`imagem.ts`:
```ts
import { LIMITES } from "./contratos";

export function dimensoesReduzidas(largura: number, altura: number, ladoMaximo = LIMITES.ladoMaximoPx) {
  const maior = Math.max(largura, altura);
  if (maior <= ladoMaximo) return { largura, altura };
  const fator = ladoMaximo / maior;
  return { largura: Math.round(largura * fator), altura: Math.round(altura * fator) };
}

/** `createImageBitmap` respeita a orientacao EXIF nos Chromes atuais; o canvas grava JPEG sem EXIF (a posicao vai em colunas proprias). */
export async function comprimirFoto(arquivo: File): Promise<{ blob: Blob; largura: number; altura: number }> {
  const bitmap = await createImageBitmap(arquivo, { imageOrientation: "from-image" });
  const { largura, altura } = dimensoesReduzidas(bitmap.width, bitmap.height);
  const canvas = new OffscreenCanvas(largura, altura);
  const ctx = canvas.getContext("2d");
  if (!ctx) throw new Error("Sem contexto 2D para comprimir a foto.");
  ctx.drawImage(bitmap, 0, 0, largura, altura);
  bitmap.close();
  const blob = await canvas.convertToBlob({ type: "image/jpeg", quality: LIMITES.qualidadeJpeg });
  return { blob, largura, altura };
}

export function capturarPosicao(): Promise<{ latitude: number; longitude: number; precisao_m: number } | null> {
  if (typeof navigator === "undefined" || !navigator.geolocation) return Promise.resolve(null);
  return new Promise((resolver) => {
    navigator.geolocation.getCurrentPosition(
      (p) => resolver({ latitude: p.coords.latitude, longitude: p.coords.longitude, precisao_m: p.coords.accuracy }),
      () => resolver(null),
      { enableHighAccuracy: true, timeout: LIMITES.gpsTimeoutMs, maximumAge: 30_000 },
    );
  });
}
```

- [ ] **Step 3: Rodar e commitar**

Run: `npx vitest run src/lib/campo` → PASS.
```bash
git add -A && git commit -m "feat(campo): contratos, banco local em IndexedDB e regras puras da fila" && git push origin main
```

---

### Task 4: API do campo

**Files:**
- Create: `web/src/lib/campo/servidor.ts`
- Create: `web/src/app/api/campo/estado/route.ts`, `web/src/app/api/campo/fotos/route.ts`, `web/src/app/api/campo/eventos/route.ts`

**Interfaces:**
- Produces:
  ```ts
  export async function equipeDaSessao(sessao: Sessao, equipePedida: number | null): Promise<{ id: number; nome: string; lider_nome: string } | { erro: string; status: number }>;
  export async function chamadoPertenceAEquipe(chamadoId: number, equipeId: number): Promise<boolean>;
  // GET  /api/campo/estado?equipe=<id>   → EstadoCampo (sem sincronizadoEm)
  // POST /api/campo/fotos (multipart)     → 201 { id, caminho } | 200 repetida
  // POST /api/campo/eventos { eventos }   → 200 { resultados: ResultadoEvento[] }
  ```

- [ ] **Step 1: Helpers**

`servidor.ts` (`server-only`): `equipeDaSessao` devolve a equipe liderada quando `cargo === "rocador"` (`sessao.equipeId`; sem equipe → `{ erro: "Você ainda não lidera uma equipe. Peça a um administrador.", status: 403 }`); para `admin`/`super_admin` exige `?equipe=` (senão `400` com "Escolha a equipe"); `analista` → `403`. `chamadoPertenceAEquipe` consulta `chamados` + `agendamentos.equipe_id`.

- [ ] **Step 2: Estado**

`estado/route.ts`: `GET`: sessão → equipe → `listarChamados` filtrado por `equipeId` e status não terminais mais `concluido`/`cancelado` com `atualizado_em` nos últimos 7 dias; mapeia para `ChamadoCampo` (o `comentario_gestor` é o `payload.comentario` do último evento `devolvido`; `adiamento_pendente` vem de `chamado_adiamentos` sem decisão); `notificacoesNaoLidas` de `contarNaoLidas`; `servidorEm: new Date().toISOString()`. Resposta com `Cache-Control: private, no-store`.

- [ ] **Step 3: Fotos**

`fotos/route.ts`: `POST` lê `await request.formData()`; valida `arquivo` (`File`, `type === "image/jpeg"`, `size ≤ 2_097_152`), `chamado_id`, `evento_id` (UUID), `etapa`, `papel`, `largura_px`, `altura_px`, `capturada_em` (ISO), GPS opcionais; sessão → equipe → `chamadoPertenceAEquipe`; idempotência: `select id, caminho from chamado_fotos where evento_id = ? and papel = ? and capturada_em = ?` → se existe, `200` com o existente; senão `db.storage.from("chamados").upload(caminho, Buffer.from(await arquivo.arrayBuffer()), { contentType: "image/jpeg", upsert: false })` com `caminho = chamados/${chamado_id}/${evento_id}/${crypto.randomUUID()}.jpg`, `insert chamado_fotos` e `201 { id, caminho }`. Erro de rede do Storage → `502` com mensagem.

- [ ] **Step 4: Eventos**

`eventos/route.ts`: `POST` lê `{ eventos: EventoCampo[] }` (máx. 50); sessão → equipe; para cada evento em ordem: `chamadoPertenceAEquipe` (senão `recusado` "chamado de outra equipe"); se já existe `chamado_eventos.evento_id` → `repetido`; senão `db.rpc("registrar_evento_chamado", { ..., p_origem: "campo", p_ocorrido_em: evento.ocorrido_em })`; erro `P0003` → `fora_de_ordem`; `P0001`/`P0004`/outros → `recusado` com a frase de `mensagemDoBanco` (exporte-a de `@/lib/chamados/acoes` para um módulo neutro `@/lib/chamados/erros.ts` e importe dos dois lados). Sempre `200` com a lista de resultados; a fila decide o que fazer com cada um.

- [ ] **Step 5: Testar com `curl` e commitar**

Com o cookie de um líder (copie `sb-…-auth-token` do DevTools): `curl -b "sb-mbkcygsqfcxxcmvkuqyt-auth-token=…" http://localhost:3000/api/campo/estado` → JSON com os chamados da equipe. Sem cookie → `401` do proxy.
```bash
npm run tipos && npm run lint && git add -A && git commit -m "feat(campo): API de estado, fotos e eventos" && git push origin main
```

---

### Task 5: Motor de sincronização

**Files:**
- Create: `web/src/lib/campo/sincronizar.ts`
- Create: `web/src/app/(campo)/campo/_componentes/usar-sincronizacao.ts`

**Interfaces:**
- Consumes: `banco-local.ts`, `fila.ts`, `contratos.ts` (Task 3); API (Task 4).
- Produces:
  ```ts
  export type SituacaoRede = "online" | "offline";
  export type RelatorioSync = { enviados: number; foraDeOrdem: number; recusados: number; pendentes: number; erro: string | null };
  export async function baixarEstado(equipeId: number | null): Promise<EstadoCampo | null>;    // GET estado → grava no IndexedDB
  export async function sincronizarFila(opcoes: { origem: "pagina" | "worker"; equipeId?: number | null }): Promise<RelatorioSync>;
  export async function pedirSincronizacaoEmSegundoPlano(): Promise<boolean>;                    // registra a tag "campo-sincronizar"
  export function usarSincronizacao(equipeId: number | null): {
    estado: EstadoCampo | null; fila: ItemFila[]; rede: SituacaoRede; sincronizando: boolean; ultimoRelatorio: RelatorioSync | null;
    recarregar: () => Promise<void>; enviarAgora: () => Promise<void>;
    registrar: (evento: EventoCampo, fotos: FotoLocal[]) => Promise<void>;
  };
  ```

- [ ] **Step 1: Motor**

Crie `web/src/lib/campo/sincronizar.ts`:
```ts
import { atualizarItem, fotosDoEvento, gravarEstado, lerEstado, listarFila, marcarFotoEnviada, removerDaFila } from "./banco-local";
import type { EstadoCampo, ItemFila, ResultadoEvento } from "./contratos";
import { esperaAntesDaTentativa, ordenarFila } from "./fila";

/**
 * O UNICO caminho de saida do aparelho para o servidor. Roda na pagina e no
 * service worker (por isso nao importa React nem `window`; `fetch` e IndexedDB
 * existem nos dois). A ordem e sagrada: as fotos de um evento antes do evento,
 * e os eventos na ordem em que aconteceram, porque `iniciado` depois de
 * `finalizado` e recusado pelo servidor.
 */

export type RelatorioSync = { enviados: number; foraDeOrdem: number; recusados: number; pendentes: number; erro: string | null };

const semSincronizado = (e: Omit<EstadoCampo, "sincronizadoEm">): EstadoCampo => ({ ...e, sincronizadoEm: new Date().toISOString() });

export async function baixarEstado(equipeId: number | null): Promise<EstadoCampo | null> {
  const url = equipeId != null ? `/api/campo/estado?equipe=${equipeId}` : "/api/campo/estado";
  const resposta = await fetch(url, { credentials: "same-origin", cache: "no-store" });
  if (resposta.status === 401) throw new Error("sessao_expirada");
  if (!resposta.ok) throw new Error(`estado ${resposta.status}`);
  const estado = semSincronizado((await resposta.json()) as Omit<EstadoCampo, "sincronizadoEm">);
  await gravarEstado(estado);
  return estado;
}

async function enviarFotos(item: ItemFila): Promise<void> {
  for (const foto of await fotosDoEvento(item.evento_id)) {
    if (foto.enviada) continue;
    const corpo = new FormData();
    corpo.set("arquivo", foto.blob, `${foto.foto_id}.jpg`);
    corpo.set("chamado_id", String(foto.chamado_id));
    corpo.set("evento_id", foto.evento_id);
    corpo.set("etapa", foto.etapa);
    corpo.set("papel", foto.papel);
    corpo.set("largura_px", String(foto.largura_px));
    corpo.set("altura_px", String(foto.altura_px));
    corpo.set("capturada_em", foto.capturada_em);
    if (foto.latitude != null) corpo.set("latitude", String(foto.latitude));
    if (foto.longitude != null) corpo.set("longitude", String(foto.longitude));
    if (foto.precisao_m != null) corpo.set("precisao_m", String(foto.precisao_m));

    const resposta = await fetch("/api/campo/fotos", { method: "POST", body: corpo, credentials: "same-origin" });
    if (resposta.status === 401) throw new Error("sessao_expirada");
    if (!resposta.ok) throw new Error(`foto ${resposta.status}: ${(await resposta.text()).slice(0, 120)}`);
    await marcarFotoEnviada(foto.foto_id);
  }
}

export async function sincronizarFila(opcoes: { origem: "pagina" | "worker"; equipeId?: number | null }): Promise<RelatorioSync> {
  const relatorio: RelatorioSync = { enviados: 0, foraDeOrdem: 0, recusados: 0, pendentes: 0, erro: null };
  const fila = ordenarFila(await listarFila());
  const agora = Date.now();

  for (const item of fila) {
    const podeTentarEm = new Date(item.criado_em).getTime() + esperaAntesDaTentativa(item.tentativas);
    if (item.tentativas > 0 && agora < podeTentarEm && opcoes.origem === "pagina") continue;

    try {
      await enviarFotos(item);
      const resposta = await fetch("/api/campo/eventos", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        credentials: "same-origin",
        body: JSON.stringify({ eventos: [{ evento_id: item.evento_id, chamado_id: item.chamado_id, tipo: item.tipo, payload: item.payload, ocorrido_em: item.ocorrido_em }] }),
      });
      if (resposta.status === 401) throw new Error("sessao_expirada");
      if (!resposta.ok) throw new Error(`eventos ${resposta.status}`);

      const { resultados } = (await resposta.json()) as { resultados: ResultadoEvento[] };
      const r = resultados[0];
      if (r.situacao === "aplicado" || r.situacao === "repetido") {
        await removerDaFila(item.evento_id);
        relatorio.enviados += 1;
      } else if (r.situacao === "fora_de_ordem") {
        // O servidor guardou o evento e avisou o gestor. Sai da fila: reenviar nao muda nada.
        await removerDaFila(item.evento_id);
        relatorio.foraDeOrdem += 1;
      } else {
        // Recusado por regra (ex.: fotos faltando). Fica na fila com o motivo, para a pessoa ver e corrigir.
        await atualizarItem({ ...item, tentativas: item.tentativas + 1, ultimo_erro: r.erro ?? "recusado" });
        relatorio.recusados += 1;
      }
    } catch (e) {
      const mensagem = e instanceof Error ? e.message : "falha de rede";
      if (mensagem === "sessao_expirada") {
        relatorio.erro = "sessao_expirada";
        break; // nada mais vai passar; a tela pede login e a fila fica intacta
      }
      await atualizarItem({ ...item, tentativas: item.tentativas + 1, ultimo_erro: mensagem });
      relatorio.erro = mensagem;
      break; // sem rede: parar e tentar de novo depois, mantendo a ordem
    }
  }

  relatorio.pendentes = (await listarFila()).length;
  if (relatorio.erro !== "sessao_expirada" && relatorio.enviados > 0) {
    try {
      await baixarEstado(opcoes.equipeId ?? null);
    } catch {
      // o estado velho continua valendo; a proxima abertura tenta de novo
    }
  }
  return relatorio;
}

/** Background Sync: o Chrome acorda o worker quando a rede voltar. Falha em silencio onde nao existe (iOS). */
export async function pedirSincronizacaoEmSegundoPlano(): Promise<boolean> {
  try {
    const registro = await navigator.serviceWorker?.ready;
    const sync = (registro as unknown as { sync?: { register(tag: string): Promise<void> } } | undefined)?.sync;
    if (!sync) return false;
    await sync.register("campo-sincronizar");
    return true;
  } catch {
    return false;
  }
}
```

- [ ] **Step 2: Hook da tela**

Crie `web/src/app/(campo)/campo/_componentes/usar-sincronizacao.ts`:
```ts
"use client";

import { useCallback, useEffect, useRef, useState } from "react";

import { enfileirar, guardarFoto, lerEstado, listarFila } from "@/lib/campo/banco-local";
import type { EstadoCampo, EventoCampo, FotoLocal, ItemFila } from "@/lib/campo/contratos";
import { baixarEstado, pedirSincronizacaoEmSegundoPlano, sincronizarFila, type RelatorioSync } from "@/lib/campo/sincronizar";

export type SituacaoRede = "online" | "offline";

/**
 * Estado da tela = IndexedDB + fila, nunca a rede diretamente. A rede so
 * alimenta o IndexedDB. Assim a tela e a mesma com ou sem sinal; o que muda e
 * o indicador.
 */
export function usarSincronizacao(equipeId: number | null) {
  const [estado, setEstado] = useState<EstadoCampo | null>(null);
  const [fila, setFila] = useState<ItemFila[]>([]);
  const [rede, setRede] = useState<SituacaoRede>("online");
  const [sincronizando, setSincronizando] = useState(false);
  const [ultimoRelatorio, setUltimoRelatorio] = useState<RelatorioSync | null>(null);
  const emCurso = useRef(false);

  const relerLocal = useCallback(async () => {
    const [e, f] = await Promise.all([lerEstado(), listarFila()]);
    setEstado(e);
    setFila(f);
  }, []);

  const enviarAgora = useCallback(async () => {
    if (emCurso.current || typeof navigator !== "undefined" && !navigator.onLine) return;
    emCurso.current = true;
    setSincronizando(true);
    try {
      const relatorio = await sincronizarFila({ origem: "pagina", equipeId });
      setUltimoRelatorio(relatorio);
      if (relatorio.pendentes === 0 && relatorio.erro == null) {
        await baixarEstado(equipeId).catch(() => null);
      }
    } finally {
      emCurso.current = false;
      setSincronizando(false);
      await relerLocal();
    }
  }, [equipeId, relerLocal]);

  const recarregar = useCallback(async () => {
    try {
      await baixarEstado(equipeId);
    } catch {
      // sem rede: fica o que esta no aparelho
    }
    await relerLocal();
  }, [equipeId, relerLocal]);

  const registrar = useCallback(
    async (evento: EventoCampo, fotos: FotoLocal[]) => {
      for (const foto of fotos) await guardarFoto(foto);
      await enfileirar({ ...evento, fotos: fotos.map((f) => f.foto_id), tentativas: 0, ultimo_erro: null, criado_em: new Date().toISOString() });
      await relerLocal();
      await pedirSincronizacaoEmSegundoPlano();
      void enviarAgora();
    },
    [enviarAgora, relerLocal],
  );

  useEffect(() => {
    setRede(navigator.onLine ? "online" : "offline");
    void relerLocal().then(() => (navigator.onLine ? enviarAgora() : undefined));

    const online = () => { setRede("online"); void enviarAgora(); };
    const offline = () => setRede("offline");
    const mensagem = (e: MessageEvent) => { if (e.data?.tipo === "sincronizar") void enviarAgora(); };
    const visivel = () => { if (document.visibilityState === "visible" && navigator.onLine) void enviarAgora(); };

    window.addEventListener("online", online);
    window.addEventListener("offline", offline);
    document.addEventListener("visibilitychange", visivel);
    navigator.serviceWorker?.addEventListener("message", mensagem);
    return () => {
      window.removeEventListener("online", online);
      window.removeEventListener("offline", offline);
      document.removeEventListener("visibilitychange", visivel);
      navigator.serviceWorker?.removeEventListener("message", mensagem);
    };
  }, [enviarAgora, relerLocal]);

  return { estado, fila, rede, sincronizando, ultimoRelatorio, recarregar, enviarAgora, registrar };
}
```

- [ ] **Step 3: Tipos e commit**

```bash
npm run tipos && npm run lint && git add -A && git commit -m "feat(campo): motor de sincronizacao com fila, Background Sync e hook da tela" && git push origin main
```

---

### Task 6: As telas do campo

**Files:**
- Modify: `web/src/app/(campo)/campo/_componentes/app-campo.tsx`
- Create: `cabecalho-campo.tsx`, `indicador-sincronizacao.tsx`, `lista-chamados.tsx`, `cartao-chamado.tsx`, `detalhe-chamado.tsx`, `captura-foto.tsx`, `fluxo-iniciar.tsx`, `fluxo-finalizar.tsx`, `fluxo-adiar.tsx`, `tela-revisao.tsx` (mesma pasta)

**Interfaces:**
- Consumes: `usarSincronizacao` (Task 5), `agruparChamados`, `aplicarPendencias`, `resumoPendencias` (Task 3), `comprimirFoto`, `capturarPosicao` (Task 3), `acoesDisponiveis` (Fase 2), `STATUS_CHAMADO_TOKEN`, `MOTIVO_ADIAMENTO`, `fmt`, `isoHoje` (calculado no cliente aqui, porque a página é estática: use `hojeNoFusoDoPainel()`, que é `Intl` e roda no navegador igual).
- Produces: componentes internos; nenhuma exportação para fora do grupo.

Antes de escrever a UI, invoque a skill `frontend-design` e leia o design system em `globals.css`: a tela do campo usa os mesmos tokens (`--bg`, `--surface`, `--ink`, `--accent`, escala de status) em tema escuro fixo, mas com **escala maior**: texto base 17 px, títulos 24 px, botões `h-14` (56 px), alvos de toque `min-h-14`, radii `--radius-lg`. Nada de cartão flutuante com sombra: borda e fundo, como o painel.

- [ ] **Step 1: Casca, navegação interna e estado**

`app-campo.tsx` (`"use client"`): a tela tem uma pilha de "vistas" em estado local (não na URL, porque o app é standalone e o botão voltar do Android fecha o app se a pilha do histórico estiver vazia; use `history.pushState` com `{ vista }` ao abrir detalhe/fluxo e trate `popstate` para voltar). Vistas: `lista`, `detalhe(chamadoId)`, `iniciar(chamadoId)`, `finalizar(chamadoId)`, `adiar(chamadoId)`, `revisao`. Para Admin/Super Admin sem `equipe` na URL, mostra um seletor de equipe (a lista vem de `/api/campo/estado` respondendo `400` com `{ erro, equipes: [...] }`, acrescente isso na Task 4 se ainda não estiver) e guarda a escolha em `?equipe=` via `nuqs`. Roçador ignora o parâmetro.

Estado: `const { estado, fila, rede, sincronizando, ultimoRelatorio, recarregar, enviarAgora, registrar } = usarSincronizacao(equipeId)`; `chamados = aplicarPendencias(estado?.chamados ?? [], fila)`; `grupos = agruparChamados(chamados, hojeNoFusoDoPainel())`.

Três estados de casca antes da lista: **sem estado local e sem rede** ("Conecte-se uma vez para baixar os chamados da sua equipe", com botão "Tentar de novo"); **sessão expirada** (`ultimoRelatorio?.erro === "sessao_expirada"`: "Sua sessão venceu. Entre de novo para enviar o que está guardado", botão que leva a `/entrar?proximo=/campo`; a fila **não** é apagada); **carregando** (esqueleto de três cartões).

- [ ] **Step 2: Cabeçalho e indicador**

`cabecalho-campo.tsx`: nome da equipe em `text-xl font-semibold`, "líder: {nome}" abaixo, à direita o sino (contagem de `estado.notificacoesNaoLidas`, abre lista simples vinda do estado) e o menu "Sair" (chama `sair()` da Fase 1 **depois** de `limparTudo()` e de apagar os caches: `caches.keys().then(ks => Promise.all(ks.map(k => caches.delete(k))))`). Se `fila.length > 0`, "Sair" pede confirmação: "Há N registros ainda não enviados. Se sair agora eles serão apagados."

`indicador-sincronizacao.tsx`: faixa fixa no topo, sempre visível, três estados com ícone + texto (nunca só cor): `good` "Tudo enviado · atualizado às hh:mm"; `warning` "N pendentes de envio" com botão "Enviar agora" quando `rede === "online"`; `neutro` com ícone `WifiOff` "Sem sinal · guardando no aparelho" quando `rede === "offline"`. Item com `ultimo_erro` recusado (não de rede) vira uma linha `critical` abaixo com o motivo e o número do chamado, porque isso exige ação da pessoa.

- [ ] **Step 3: Lista e cartão**

`lista-chamados.tsx`: seções na ordem *Hoje*, *Atrasados*, *Próximos*, *Aguardando aprovação*, *Concluídos recentes*; título da seção em `text-xs uppercase tracking-wide text-ink-3` com a contagem; seção vazia não aparece, exceto *Hoje*, que mostra "Nenhuma roçada para hoje". Puxar para baixo não recarrega (evita conflito com scroll); há um botão "Atualizar" no topo que chama `recarregar`.

`cartao-chamado.tsx`: botão inteiro (`min-h-14`, `rounded-lg border bg-surface p-4 text-left`), com número em `font-mono text-sm text-ink-3`, rodovia em `text-lg font-semibold`, faixa de km e sentido, chip do estado (`STATUS_CHAMADO_TOKEN`, ícone + rótulo), data prevista com `relativoEmDias`, e, quando o chamado tem item na fila, selo `Chip tom="warning"` "aguardando envio". Prioridade `critica`/`alta` ganha `ChipRisco` à direita.

- [ ] **Step 4: Detalhe**

`detalhe-chamado.tsx`: cabeçalho com "voltar", número e chip de estado. Blocos:
- Trecho: rodovia, faixa, sentido, extensão (`fmt.km`), espécie (`ESPECIE[especie].rotulo`), limite (`fmt.cm`).
- Altura inicial: valor e a frase "informada pelo gestor" ou "prevista pelo modelo"; quando o chamado está `devolvido`, um `Aviso tom="serious"` com `comentario_gestor` acima de tudo.
- Motivo do agendamento (`justificativa`) em `text-ink-2`.
- Observações do trecho, quando houver, em `Aviso tom="info"` ("Atenção no trecho").
- Adiamento pendente, quando houver: "Você pediu adiamento ({motivo}) em dd/mm. Aguardando o gestor."
- Botões: `acoesDisponiveis(status, "rocador", true)` → `iniciar` = botão primário `h-14` "Iniciar roçada"; `finalizar` = primário "Finalizar roçada"; `pedir_adiamento` = secundário "Pedir adiamento". Em `aguardando_aprovacao`: texto "Enviado para aprovação em dd/mm hh:mm. Nada a fazer por enquanto." Em `concluido`: "Aprovado em dd/mm".
- Linha do tempo resumida: últimos 5 eventos vindos do estado (`ChamadoCampo` ganha `eventos_recentes: { tipo, ocorrido_em, autor_nome }[]` na API da Task 4).

- [ ] **Step 5: Captura de foto**

`captura-foto.tsx`: um "quadro" por foto exigida, com título ("Medida do mato, com a régua encostada", "Extensão do trecho, mostrando a faixa inteira", "Como ficou depois da roçada", "Extensão do trecho depois"), área de 4:3 com ícone de câmera e "Toque para fotografar"; `<input type="file" accept="image/*" capture="environment" className="sr-only">` acionado pelo toque. Ao escolher: `comprimirFoto(arquivo)` e `capturarPosicao()` em paralelo, prévia (`URL.createObjectURL(blob)`, revogada no `useEffect` de limpeza), legenda com tamanho (`fmt.n(Math.round(bytes/1024))} KB`) e GPS ("±12 m" ou "sem GPS"); botão "Refazer". Quadros extras: botão "Adicionar foto" até `LIMITES.fotosExtrasMax`. O componente devolve `FotoLocal[]` prontos (`foto_id` e `evento_id` recebidos por prop, `capturada_em = new Date().toISOString()` no momento da escolha, `enviada: false`).

- [ ] **Step 6: Fluxos**

`fluxo-iniciar.tsx`: `evento_id = crypto.randomUUID()` criado ao abrir a vista (e mantido se a pessoa voltar e avançar); dois `CapturaFoto` (`papel` `medida` e `extensao`, `etapa` `inicio`) + extras; botão "Revisar" habilita só com as duas; `TelaRevisao` mostra as miniaturas e o texto "Ao confirmar, o chamado passa a Em andamento. Sem sinal, fica guardado e é enviado depois." Confirmar → `registrar({ evento_id, chamado_id, tipo: "iniciado", payload: {}, ocorrido_em }, fotos)` → volta ao detalhe com toast "Guardado no aparelho" ou "Enviado" conforme `rede`.

`fluxo-finalizar.tsx`: igual, `papel` `resultado` e `extensao`, `etapa` `fim`, mais `Campo` "Altura final do mato (cm)" com `Entrada type="number" inputMode="decimal"` grande (`h-14 text-xl tnum`), aceitando vírgula, validando 0–300; payload `{ altura_final_cm }`.

`fluxo-adiar.tsx`: lista de motivos como botões de rádio grandes (`MOTIVO_ADIAMENTO`), `AreaTexto` "O que aconteceu" (opcional, obrigatório se motivo `outro`), `Entrada type="date"` "Quando dá para voltar" (opcional, mínimo amanhã), revisão e "Pedir adiamento"; payload `{ motivo, detalhe, data_sugerida }`.

`tela-revisao.tsx`: componente genérico com título, filhos (miniaturas ou resumo), a frase de consequência, e dois botões: primário "Confirmar" (`carregando` enquanto grava no IndexedDB) e fantasma "Voltar".

- [ ] **Step 7: Verificar no Chrome e commitar**

`npm run build && npm run start`; DevTools em modo dispositivo (Pixel 7, 412×915); entrar como `lider.1.demo@…`; `/campo`: lista com os chamados do seed da equipe 1; Network → Offline; abrir um `aberto`, Iniciar com duas imagens do disco (o `input` aceita arquivo), confirmar → estado otimista "Em andamento", indicador "2 pendentes · sem sinal"; Network → Online → indicador vai a "Tudo enviado"; em outra aba, `/chamados` mostra o chamado em andamento com as duas fotos. Tema: só escuro. Contraste dos textos sobre `--surface` no escuro já foi medido no design system.
```bash
npm run verificar && git add -A && git commit -m "feat(campo): telas de lista, detalhe, iniciar, finalizar e adiar com fila offline" && git push origin main
```

---

### Task 7: Deploy, APK pelo PWABuilder e verificação de propriedade

**Files:**
- Create: `web/public/.well-known/assetlinks.json`
- Create: `docs/operacao/apk.md`

- [ ] **Step 1: Deploy de produção**

Mesmo procedimento da Fase 1, Task 12, Step 4 (`git archive` + `vercel --prod --yes`). Depois: `https://motiva-highwai.vercel.app/campo` logado como líder do seed abre a lista; Lighthouse (Chrome DevTools → Lighthouse → categoria PWA/"Instalável") sem erro de manifesto nem de service worker.

- [ ] **Step 2: Gerar o pacote**

Em `https://www.pwabuilder.com`, informar `https://motiva-highwai.vercel.app/campo` → "Package for stores" → **Android**:
- Package ID `br.com.highwai.campo`; App name `HighwAI Campo`; Launcher name `Campo`; App version `0.1.0`, version code `1`.
- Display mode `standalone`; Status bar color `#0a0d0c`; Splash: background `#0a0d0c`, ícone 512.
- **Signing key: "Create new"**, preencher alias `highwai-campo`, senhas fortes, organização "HighwAI"; anotar tudo.
- Start URL `/campo`; Fallback behavior `Custom Tabs`; Notifications `enabled` (não custa nada e deixa o Web Push da Fase 5 possível).
- Gerar e baixar o zip.

- [ ] **Step 3: Guardar a chave e publicar o `assetlinks.json`**

O zip traz `app-release-signed.apk`, `app-release-bundle.aab`, `signing.keystore`, `signing-key-info.txt` e `assetlinks.json`. Guardar `signing.keystore` e `signing-key-info.txt` no cofre de senhas (não no repositório, não em E:). Copiar `assetlinks.json` para `web/public/.well-known/assetlinks.json`; conferir que o `package_name` e o `sha256_cert_fingerprints` batem com o `signing-key-info.txt`. Redeployar. Verificar: `curl -s https://motiva-highwai.vercel.app/.well-known/assetlinks.json` devolve o JSON com `Content-Type: application/json`.

- [ ] **Step 4: Documentar**

Crie `docs/operacao/apk.md` com: o que é TWA em duas frases; o procedimento acima; **como atualizar** (o conteúdo atualiza sozinho porque é a web; só se gera APK novo ao trocar ícone, nome, domínio ou permissões, e aí é obrigatório "Use existing key" com o `signing.keystore` guardado, senão o Android recusa a atualização); onde o `assetlinks.json` mora e por que; e o aviso de que trocar de domínio exige APK novo e `assetlinks.json` novo.

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "feat(campo): assetlinks do APK e guia de operacao" && git push origin main
```

---

### Task 8: Distribuição do APK

**Files:**
- Create: `web/src/app/(painel)/usuarios/_componentes/baixar-app.tsx`
- Modify: `web/src/app/(painel)/usuarios/page.tsx`, `web/src/app/(campo)/campo/_componentes/cabecalho-campo.tsx`

- [ ] **Step 1: Release no GitHub**

Renomear `app-release-signed.apk` para `campo.apk`, calcular `sha256sum campo.apk`, e criar a release `campo-v0.1.0` no repositório `Moreettoo/motiva` pela interface web do GitHub (o `gh` não está instalado), anexando `campo.apk` e escrevendo o SHA-256 na descrição. O link fixo `https://github.com/Moreettoo/motiva/releases/latest/download/campo.apk` passa a apontar para ele; é o link do QR gerado na Task 1. Se o repositório for privado, o link exige login no GitHub: nesse caso, publique também o APK no bucket `apk` do Supabase Storage (público, só este arquivo) e troque o link no QR e no cartão.

- [ ] **Step 2: Cartão "Baixar o app de campo"**

`baixar-app.tsx` (Server Component): `Cartao` com o QR (`<img src="/icones/qr-apk.svg" alt="QR code para baixar o app de campo" width={160} height={160}>`), o link em texto, a versão e o SHA-256 (em `font-mono text-xs`), e os passos numerados: 1) abrir o link no celular Android; 2) permitir "instalar apps de fontes desconhecidas" para o Chrome quando o Android pedir; 3) abrir o app e entrar com o e-mail e a senha do convite; 4) a primeira abertura precisa de sinal. Renderizado no fim de `/usuarios`. Em `cabecalho-campo.tsx`, quando `window.matchMedia("(display-mode: browser)").matches` (não está instalado), um link discreto "Instalar o app" que abre o mesmo cartão em `PainelLateral`.

- [ ] **Step 3: Commit**

```bash
npm run verificar && git add -A && git commit -m "feat(campo): QR e instrucoes de instalacao do APK" && git push origin main
```

---

### Task 9: Verificação no Android real

**Files:** nenhum novo. Correções como `fix(campo): ...`.

- [ ] **Step 1: Instalar**

No Android: abrir o link do QR, baixar `campo.apk`, permitir a instalação, abrir. Esperado: tela cheia sem a barra do Chrome (se a barra aparecer, o `assetlinks.json` não bateu: conferir fingerprint e `package_name`, e que a URL foi servida como JSON). Entrar como `lider.1.demo@…`.

- [ ] **Step 2: Roteiro offline**

| Passo | Esperado |
|---|---|
| Fechar o app, ligar o modo avião, reabrir | lista aparece do IndexedDB; indicador "Sem sinal" |
| Iniciar um chamado com duas fotos da câmera | prévia mostra "±N m"; confirmar → "Guardado no aparelho"; estado "Em andamento"; indicador "2 pendentes" |
| Finalizar com duas fotos e altura 7,5 | "4 pendentes"; estado "Aguardando aprovação" |
| Pedir adiamento em outro chamado (chuva, amanhã) | "5 pendentes" |
| Desligar o modo avião **com o app fechado**, esperar 1 min, abrir o painel no computador | `/chamados` mostra os três chamados atualizados (Background Sync enviou) — se não, abrir o app: ele envia ao ficar visível |
| No app | indicador "Tudo enviado"; sino conta a devolução ou aprovação feita no painel |
| No painel, aprovar o finalizado com reanálise | página do trecho mostra execução e medição novas |
| Sessão: no painel, desativar o líder; no app, tentar iniciar outro chamado | envio falha com "Sua sessão venceu"; a fila permanece; reativar e entrar de novo envia |
| Cancelar um chamado no painel enquanto o app está em modo avião com um `iniciado` pendente; voltar o sinal | app mostra o item como "registrado fora de ordem"; painel mostra o evento `fora_de_ordem` e o sino do admin conta 1 |

- [ ] **Step 3: Commit final da fase**

```bash
git add -A && git commit -m "fix(campo): achados da verificacao no Android" && git push origin main
```

---

## Auto-revisão do plano

- **Cobertura do spec §3:** onde vive e por que abre sem sinal (T2), service worker (T2), dados locais (T3), captura e compressão (T3, T6), sincronização (T5), API (T4), telas (T6), APK (T7), distribuição (T8), roteiro no Android (T9).
- **Consistência:** `registrar_evento_chamado` com `p_origem: "campo"` (T4) é a mesma função da Fase 2; `fora_de_ordem` devolve `P0003` → `situacao: "fora_de_ordem"` (T4) → sai da fila (T5) → selo na tela (T6). `evento_id`/`foto_id` nascem em T6 (`crypto.randomUUID()`), viajam em T5, são a chave de idempotência em T4. `LIMITES` (T3) é lido por T4 (2 MB), T6 (4 extras, 300 cm).
- **Cortes para domingo, se apertar:** T7/T8 viram "Adicionar à tela inicial" no Chrome (a PWA já é instalável depois da T2); em T6, o fluxo de adiamento pode ficar para depois da demonstração; o seletor de equipe para Admin pode ser um `?equipe=` digitado na URL.
